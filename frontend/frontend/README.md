# Construction Site - Frontend

## Local setup

1. Copy `.env.example` to `.env`
2. Set `VITE_API_BASE_URL` to your backend URL
3. Run:

```bash
npm install
npm run dev
```

## Admin Login

- Username: `admin`
- Password: `admin123`
